import 'package:flutter/material.dart';

class ZonesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Zonas'),
      ),
      body: Center(
        child: Text('Aquí se mostrarán las Zonas'),
      ),
    );
  }
}
